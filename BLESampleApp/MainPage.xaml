<Page
    x:Class="BLESampleApp.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BLESampleApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="1*" />
        </Grid.ColumnDefinitions>

        <!-- Left half for Server -->
        <Grid Grid.Column="0" Padding="10">
            <TextBlock Text="Server" FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>

            <StackPanel Orientation="Vertical" Margin="0,40,0,20">
                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                    <Button x:Name="StartServerButton" Content="Start Server" Click="StartServerButton_Click" Margin="0,0,10,0"/>
                    <Button x:Name="StopServerButton" Content="Stop Server" Click="StopServerButton_Click" IsEnabled="False"/>
                </StackPanel>

                <TextBlock Text="Server Status" Margin="0,5"/>
                <TextBlock x:Name="ServerStatusText" Text="Server: Stopped" Margin="0,5"/>
                <TextBlock x:Name="ClientCountText" Text="Connected Clients: 0" Margin="0,5"/>

                <TextBlock Text="Messages Received:" Margin="0,10,0,5"/>
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <TextBox x:Name="ServerReceivedMessagesTextBox" IsReadOnly="True" TextWrapping="Wrap" AcceptsReturn="True" Height="200"/>
                </ScrollViewer>

                <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                    <TextBox x:Name="ServerMessageTextBox" PlaceholderText="Type message to send..." Width="300" Margin="0,0,10,0"/>
                    <Button x:Name="ServerSendButton" Content="Send" Click="ServerSendButton_Click" IsEnabled="False"/>
                </StackPanel>
            </StackPanel>
        </Grid>

        <!-- Middle divider line -->
        <Grid Grid.Column="1" Background="Gray" Width="1" />

        <!-- Right half for Client -->
        <Grid Grid.Column="2" Padding="10">
            <TextBlock Text="Client" FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>

            <!--<ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <StackPanel Orientation="Vertical" Margin="0,40,0,20">
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                        <Button x:Name="StartScanButton" Content="Start Scanning" Click="StartScanButton_Click" Margin="0,0,10,0"/>
                        <Button x:Name="StopScanButton" Content="Stop Scanning" Click="StopScanButton_Click" IsEnabled="False"/>
                    </StackPanel>

                    <TextBlock Text="Scan Status" Margin="0,5"/>
                    <TextBlock x:Name="ScanStatusText" Text="Scanner: Stopped" Margin="0,5"/>

                    <TextBlock Text="Available Devices:" Margin="0,10,0,5"/>
                    <ListView x:Name="DeviceListView" Height="150" BorderBrush="Gray" BorderThickness="1">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding}"/>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>

                    <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                        <Button x:Name="ConnectButton" Content="Connect" Click="ConnectButton_Click" IsEnabled="False" Margin="0,0,10,0"/>
                        <Button x:Name="DisconnectButton" Content="Disconnect" Click="DisconnectButton_Click" IsEnabled="False"/>
                    </StackPanel>

                    <TextBlock x:Name="ConnectionStatusText" Text="Status: Disconnected" Margin="0,5"/>

                    <TextBlock Text="Messages Received:" Margin="0,10,0,5"/>
                    <TextBox x:Name="ClientReceivedMessagesTextBox" IsReadOnly="True" TextWrapping="Wrap" AcceptsReturn="True" Height="150"/>

                    <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                        <TextBox x:Name="ClientMessageTextBox" PlaceholderText="Type message to send..." Width="300" Margin="0,0,10,0"/>
                        <Button x:Name="ClientSendButton" Content="Send" Click="ClientSendButton_Click" IsEnabled="False"/>
                    </StackPanel>
                </StackPanel>
            </ScrollViewer>-->

            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <Button x:Name="ScanButton" Content="Start Scanning" Click="ScanButton_Click" Margin="10"/>
                <ListView x:Name="DevicesList" Height="200" Width="250" SelectionChanged="DevicesList_SelectionChanged"/>
                <Button x:Name="ConnectButton" Content="Connect" Click="ConnectButton_Click" Margin="10" IsEnabled="False"/>

                <TextBox x:Name="MessageBox" Width="250" Height="30" PlaceholderText="Enter message"/>
                <Button x:Name="SendButton" Content="Send Message" Click="SendButton_Click" Margin="10" IsEnabled="False"/>
                <TextBlock x:Name="ReceivedMessage" Text="Received: " FontSize="16"/>
            </StackPanel>
        </Grid>
    </Grid>


</Page>
